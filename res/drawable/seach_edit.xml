<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle"
    android:padding="@dimen/search_shape_padding">
    <solid android:color="@color/white_gray"/>
    <corners
        android:bottomRightRadius="@dimen/search_round"
        android:bottomLeftRadius="@dimen/search_round"
        android:topLeftRadius="@dimen/search_round"
        android:topRightRadius="@dimen/search_round"/>
</shape>
